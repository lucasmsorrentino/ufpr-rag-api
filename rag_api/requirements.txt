# torch CPU-only: o wheel padrão do PyPI traz bibliotecas CUDA (~7 GB) que são
# inúteis nestas VMs (Oracle Always Free, sem GPU). O índice abaixo entrega a
# build de CPU e derruba a imagem de ~8.8 GB para ~2 GB.
--extra-index-url https://download.pytorch.org/whl/cpu

# API
fastapi==0.120.4
uvicorn[standard]==0.46.0
pydantic==2.12.5

# RAG — lancedb pinado no formato que gerou o store (0.30.2)
lancedb==0.30.2
sentence-transformers==5.4.1
torch==2.11.0
numpy==2.4.4
